{
  "name": "@nextdog/ui",
  "version": "0.1.0",
  "description": "NextDog dashboard UI — Preact-based observability viewer",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "panda codegen",
    "dev": "panda codegen && vite",
    "build": "panda codegen && vite build",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests",
    "prepublishOnly": "pnpm build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@ark-ui/react": "^5.34.1",
    "preact": "^10",
    "preact-router": "^4"
  },
  "devDependencies": {
    "@pandacss/dev": "^1.9.1",
    "@preact/preset-vite": "^2",
    "postcss": "^8.5.8",
    "vite": "^6",
    "vitest": "^3"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/cscherban/NextDog"
  }
}
