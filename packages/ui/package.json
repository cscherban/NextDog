{
  "name": "@nextdog/ui",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "preact": "^10",
    "preact-router": "^4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2",
    "vite": "^6",
    "vitest": "^3"
  }
}
